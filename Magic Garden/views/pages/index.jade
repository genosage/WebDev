extends ../layout

block content
  .container
    .row
      each item in tickets
        .col-md-2
          .thumbnail
            a(href="/ticket/#{item._id}")
              img(src="#{item.image}", alt="#{item.title}")
            .caption
              h3 #{title}
              p: a.btn.btn-primary(href="/ticket/#{item._id}", role="button")
                •Ticket detail